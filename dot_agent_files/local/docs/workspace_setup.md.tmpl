{{- /* This machine's workspace-setup doc, pulled from the private repo by
       machine_name. Public source names no machine; renders empty without the
       private repo. */ -}}
{{- $f := joinPath .chezmoi.sourceDir "dotfiles_private" "dot_agent_files" (index . "machine_name" | default "") "docs" "workspace_setup.md" -}}
{{- if and (ne (index . "machine_name" | default "") "") (stat $f) -}}
{{ include $f }}
{{- end -}}
