One sentence explaining what this PR does.

Do not include personal paths, machine-specific env names, display values,
local usernames, or personal command style in this description. Prefer
repo-relative commands and neutral placeholders when commands are needed.

Follow this format by default, but you can deviate if that makes the PR
clearer, for example for an unusually large PR.

## Related:
Optional. Avoid unless truly needed. Link related issues, docs, PRs, or
discussion.
- ...

## Changes:
What changed in the final state of the PR. Describe the current behavior the
reader needs to know. Do not narrate the whole implementation history of the PR.
Be concise.
- ...
- ...

## Why:
What problem this solves and why the changes were needed.
Be concise.
- ...
- ...

## Details:
Optional. Avoid unless truly needed. Use this for important details or gotchas
that would clutter the changes section.
- ...
- ...

## How To Try It:
Optional. Avoid unless truly needed. Use this when a reviewer would benefit
from commands, paths, task ids, or links to inspect the change directly. Do
not repeat generic CI or test output.
- ...
