{{- $machineClass := index . "machine_class" | default "" -}}
{{/* GitHub git auth is HTTPS via the gh credential helper, so no github.com SSH
     host block is needed here. SSH config below is only for non-GitHub hosts. */}}
{{- if eq $machineClass "personal" }}

{{ include (joinPath .chezmoi.sourceDir "dotfiles_private" "private_dot_ssh" "config.personal") }}
{{- end }}
{{- if eq .chezmoi.os "darwin" }}

{{ include (joinPath .chezmoi.sourceDir "dotfiles_private" "private_dot_ssh" "config.macos") }}
{{- end }}
